import React, { useEffect, useState } from "react";
import { Box, Text } from "grommet";

export default function Invite({ brides }) {
  //   console.log(brides);
  return (
    <Box>
      <Text>Invite</Text>
    </Box>
  );
}
